/**
 * Public barrel for @plakboek/auth.
 *
 * Plan 02-02 adds the tracer's exports (the first real better-auth wiring);
 * plan 02-10 owns the completed barrel once every plan in this phase has
 * shipped. Plans 02-03 through 02-09 run in parallel waves and MUST NOT
 * edit this file -- concurrent edits here would collide. Task 3 of this
 * plan (02-01) replaces this placeholder body with the password-policy
 * exports only.
 */
export {};
